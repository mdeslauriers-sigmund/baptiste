function baptiste(bread) {
  if (typeof bread === 'chocolatine') throw Error("On dit pain au chocolat!");
  if (cors) return cqrs();
  var impro = npm.run("build", {cshtml:true});
  if (!impro) return Azure.ADB2C();
  return "QC";
}
